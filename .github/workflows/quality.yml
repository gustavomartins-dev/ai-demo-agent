name: Quality

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/ai_demo_agent_ci?schema=public
      AUTH_SECRET: ci-only-auth-secret-not-used-in-production
      AUTH_GITHUB_ID: ci-github-client-id
      AUTH_GITHUB_SECRET: ci-github-client-secret
      APP_OWNER_GITHUB_LOGIN: ci-owner
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: ai_demo_agent_ci
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d ai_demo_agent_ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Validate Prisma schema
        run: npm run db:validate

      - name: Validate production configuration contract
        run: npm run validate:production
        env:
          NODE_ENV: production
          APP_BASE_URL: https://ci.example.com
          SOCIAL_TOKEN_ENCRYPTION_KEY: MDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDA=
          SOCIAL_TOKEN_ENCRYPTION_KEY_ID: ci-v1
          X_CLIENT_ID: ci-x-client
          LINKEDIN_CLIENT_ID: ci-linkedin-client
          LINKEDIN_CLIENT_SECRET: ci-linkedin-secret
          LINKEDIN_API_VERSION: "202608"
          AI_DEMO_OUTPUT_ROOT: /var/lib/ai-demo-agent/output
          AI_DEMO_HERMES_COMMAND: /usr/local/bin/hermes

      - name: Apply migrations to clean PostgreSQL
        run: npm run db:deploy

      - name: Run engine tests
        run: npm test

      - name: Run social publishing safety gate
        run: npm run test:social-safety

      - name: Check engine TypeScript
        run: npm run check

      - name: Lint web application
        run: npm run lint:web

      - name: Check web TypeScript
        run: npm run check:web

      - name: Build web application
        run: npm run build:web

      - name: Verify owner launch journey end to end
        run: npm run test:e2e
        env:
          AUTH_URL: http://127.0.0.1:3100
          AI_DEMO_OUTPUT_ROOT: /tmp/ai-demo-agent-e2e-output

      - name: Audit production dependencies
        run: npm audit --omit=dev
